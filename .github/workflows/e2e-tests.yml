name: E2E Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: 'npm'
          cache-dependency-path: tests/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: tests

      - name: Cache Playwright browsers
        id: cache-playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('tests/package-lock.json') }}

      - name: Install Playwright Browsers
        if: steps.cache-playwright.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps
        working-directory: tests

      - name: Install Snapshot Tools
        uses: awalsh128/cache-apt-pkgs-action@v1
        with:
          packages: "graphicsmagick ghostscript"
          version: 1

#      - name: Run Playwright tests
#        run: npx playwright test e2e/export-pdf.spec.js --config=playwright.config.prod.js # TODO run all
#        working-directory: tests

#      - uses: actions/upload-artifact@v4
#        if: ${{ !cancelled() }}
#        with:
#          name: playwright-report
#          path: tests/playwright-report/
#          retention-days: 30




#      - name: Debug PDF generation inside backend container
#        run: |
#          curl -s \
#            -X POST http://localhost:8000/convert \
#            -H "Content-Type: application/json" \
#            -d '{"markdown":"# Test PDF from CI"}' \
#            -o /tmp/test.pdf
#          ls -lh /tmp/test.pdf
#          file /tmp/test.pdf
      - name: Debug PDF generation inside backend container 2
        run: |
          docker compose -f docker-compose.prod.yml up -d
          docker compose exec -T backend bash -c "
            apt-get update && apt-get install -y curl file &&
            curl -s \
            -X GET http://frontend:5173/editor \
            -H 'Content-Type: application/json'
          "
